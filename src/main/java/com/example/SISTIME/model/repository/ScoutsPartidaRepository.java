package com.example.SISTIME.model.repository;

import com.example.SISTIME.model.entity.ScoutsPartida;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScoutsPartidaRepository  extends JpaRepository<ScoutsPartida, Long> {
}
